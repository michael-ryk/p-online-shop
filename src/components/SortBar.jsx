import styled from 'styled-components';

const SortBar = () => {
  return (
    <SortBarStyles>
      <h1>Sorting Bar</h1>
    </SortBarStyles>
  )
}

const SortBarStyles = styled.section`
  text-align: left;
`

export default SortBar;